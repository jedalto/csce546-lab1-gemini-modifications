# Lab 1: Gemini Modifications

For my modification, I used the window size class to change the layout of the app depending on the orientation of the screen. In portrait mode, the content is stacked vertically, while the content is place horizontally in landscape.
